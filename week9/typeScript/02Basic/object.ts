const User = {
    name: "hieats",
    email: "sudhanshu6073@gmail.com",
    isActive: true
}

function createUser({name:string, isPaid:boolean}) {}

createUser({name: "hit", isPaid: false})

function createCourse():{} {
    return {}
}

export {}